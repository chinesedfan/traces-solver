{
  "name": "traces-solver",
  "version": "1.0.0",
  "description": "solver for the iOS game `logic traces`",
  "main": "./src/index.js",
  "scripts": {
    "test:coverage": "istanbul cover ./node_modules/mocha/bin/_mocha --report lcovonly -- -R spec && cat ./coverage/lcov.info | coveralls",
    "test": "mocha"
  },
  "keywords": [
    "logic",
    "traces",
    "iOS",
    "game",
    "solver"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/chinesedfan/traces-solver.git"
  },
  "homepage": "https://github.com/chinesedfan/traces-solver",
  "author": "Xianming Zhong",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "coveralls": "^3.0.0",
    "istanbul": "^0.4.5",
    "mocha": "^5.0.4"
  }
}
